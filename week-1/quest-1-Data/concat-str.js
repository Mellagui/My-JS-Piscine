function concatStr(a,b) {return '' + a + b};

// const concatStr = (a,b) => `${a}${b}`; // to concat between any types
// //     let x = string(a).concat(string(b)); // to concat any variables has type string
// //     return x;
// // };

// console.log(concatStr(2,3));